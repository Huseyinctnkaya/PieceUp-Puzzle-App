node_modules
build
public/build
*/*.yml
.shopify

# The reference puzzle, vendored from onurhan1337/generic-comps. It is upstream
# code we carry rather than code we write, so our rules do not apply to it —
# holding it to them would mean rewriting it, which defeats the point of using
# the reference implementation.
widget/src/components
widget/src/sub-components
widget/src/lib
widget/src/global-types.ts

# Build output of extensions/pieceup-widget/build.mjs.
extensions/pieceup-widget/assets/pieceup-app.js
extensions/pieceup-widget/assets/pieceup-app.css
